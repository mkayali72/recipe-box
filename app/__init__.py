# -----------------------------------------------------------------------------
# Recipe Box application package.
# This marker makes the FastAPI application importable inside the Docker image
# while leaving room for database, route, template, and static modules to grow.
# -----------------------------------------------------------------------------